import java.math.BigInteger;
import java.util.Random;
import java.math.*;

public class RandomGen {

	public int genRandInput(int size){
		byte[] result = new byte[128];
		
		BigInteger b;
		
		Random r = new Random();
		
		do{
			r.nextBytes(result);
			b=new BigInteger(result);
		
		}while(b < 0 && b <  )
		
		System.out.println(b);
		
		return 0;
	}
	
}
