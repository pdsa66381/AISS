import java.math.BigInteger;


public class DefaultStrategy implements PowerStrategy{

	@Override
	public BigInteger pow(BigInteger x, BigInteger e) {
		BigInteger converter = new BigInteger(x.byteValue())
		System.out.println(x.pow(e.intValue()));
		return BigInteger.ZERO;
	}

	@Override
	public BigInteger pow(BigInteger x, int e) {
		System.out.println(x.pow(e));
		return BigInteger.ZERO;
	}

}
